2. Find a two way route with maximum Desired number of hops :
Example : The number of trips starting at C and ending at C with a maximum of 3 hops. 

3. Find a two way route with exact number of hops : 
Example : The number of trips starting at C and ending at C with exact 3 hops. 

4. Find a route between two houses with maximum Desired number of hops
Example : The number of trips starting at A and ending at C with a maximum of 3 hops.

5. Find a route between two houses with exact number of hops
Example : The number of trips starting at C and ending at C with exact 3 hops. 